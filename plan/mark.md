react / react-dom          — 框架核心
radix-ui                   — UI 原语
class-variance-authority   — 组件变体
clsx + tailwind-merge      — 样式工具
lucide-react               — 图标
react-markdown + remark-gfm — Markdown 渲染
react-syntax-highlighter   — 代码语法高亮
zustand                    — 状态管理 ← 新增
framer-motion              — 动画 ← 新增
react-router-dom           — 路由 ← 新增
axios                      — HTTP 请求 ← 新增
@tanstack/react-query      — 异步状态管理 ← 新增
@microsoft/fetch-event-source — SSE 流式请求 ← 新增